# ===========================================
# ACE SERVICE GROUP - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in the values

# ===========================================
# DATABASE (Required)
# ===========================================
# Vercel Postgres connection string
# Get these from your Vercel Postgres dashboard
DATABASE_URL="postgres://..."
DATABASE_URL_UNPOOLED="postgres://..."

# ===========================================
# AUTHENTICATION (Required)
# ===========================================
# NextAuth secret - generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"

# Auth URL (set automatically by Vercel, but needed locally)
AUTH_URL="http://localhost:3000"

# ===========================================
# ADMIN SEED (Required for initial setup)
# ===========================================
# These are used to create the first admin user
# Run: npm run db:seed after setting these
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="your-secure-password"
ADMIN_NAME="Admin"

# ===========================================
# FILE UPLOADS (Required for image uploads)
# ===========================================
# Vercel Blob storage token
# Get this from your Vercel Blob dashboard
BLOB_READ_WRITE_TOKEN="vercel_blob_..."

# ===========================================
# OPTIONAL
# ===========================================
# Node environment
NODE_ENV="development"
